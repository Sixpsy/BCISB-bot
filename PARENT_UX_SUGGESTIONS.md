# ข้อเสนอแนะ — ปรับบอทให้เหมาะสำหรับผู้ปกครองที่ไม่คุ้นเคยกับ Discord

---

## 🐛 Bugs ที่แก้ไขแล้วในรอบนี้

| # | ปัญหา | ผลกระทบ | สถานะ |
|---|--------|----------|--------|
| 1 | `CAT_COLORS["academic"]` crash — fallback สีใช้ key "academic" ซึ่งถูกลบออกจาก categories.json แล้ว | ปฏิทินไม่แสดงเลยเมื่อมี event ที่ category ไม่ match | ✅ แก้แล้ว |
| 2 | `/add-event` และ `/add-recurring` มี dropdown แบบ hardcode ยังเป็น academic/holiday/exam/activity/parent — ไม่อ่านจาก categories.json | Admin เลือก category ใหม่ไม่ได้ ต้องพิมพ์เอง และยังโชว์ค่าเก่า | ✅ แก้แล้ว (เปลี่ยนเป็น autocomplete) |
| 3 | ป้ายกำกับ category ในข้อความแจ้งเตือนรายวันและ DM reminder ยังเป็น dict hardcode เก่า | category ใหม่เช่น "register" จะแสดงเป็น key ดิบแทนภาษาไทย | ✅ แก้แล้ว |
| 4 | `AddEventModal.on_submit` ตรวจสอบ `CAT_VALID` จากตอน startup แทนที่จะโหลดใหม่ | แก้ categories.json แล้วไม่ต้อง restart bot ก็ได้ แต่ modal validation จะยังใช้ค่าเก่า | ✅ แก้แล้ว |

---

## 💡 ข้อเสนอแนะ — แบ่งเป็น 3 ระดับ

---

### 🟢 ระดับ 1 — ทำได้เลย ได้ผลชัดเจน (ไม่ซับซ้อน)

#### 1. โครงสร้าง Server ที่ชัดเจน
จัดช่อง (channel) ให้ชัดเจนก่อนเพิ่มผู้ปกครอง เพราะผู้ปกครองที่ไม่คุ้นกับ Discord จะสับสนถ้าเห็นช่องเยอะเกินไป

แนะนำโครงสร้างนี้:
```
📌 ยินดีต้อนรับ & วิธีใช้งาน   (read-only)
📅 ปฏิทินกิจกรรม               (read-only — บอทโพสต์ที่นี่)
👗 ชุดนักเรียน                  (read-only — บอทโพสต์ที่นี่)
📂 เอกสารโรงเรียน              (read-only — บอทโพสต์ที่นี่)
💬 ถามตอบทั่วไป               (ผู้ปกครองพูดคุยได้)
```
ตั้งทุกช่องเป็น read-only สำหรับผู้ปกครอง ยกเว้น "ถามตอบ" เพื่อป้องกันการส่งข้อความผิดช่อง

---

#### 2. เพิ่ม `/help` command
ผู้ปกครองจะไม่รู้ว่ามีคำสั่งอะไรบ้าง ถ้าไม่บอก คำสั่งนี้ควรตอบเป็นภาษาไทยเข้าใจง่าย เช่น:

```
📅 /calendar — ดูปฏิทินกิจกรรม 2 เดือน
🔔 /remind   — ตั้งแจ้งเตือนส่วนตัวก่อนถึงวันกิจกรรม
```

แค่ 2 คำสั่งนี้พอสำหรับผู้ปกครอง ไม่ต้องอธิบายคำสั่ง Admin

---

#### 3. ข้อความต้อนรับใน "ยินดีต้อนรับ" channel
Pin ข้อความในช่อง #ยินดีต้อนรับ อธิบายสั้นๆ ว่าบอททำอะไรได้บ้าง เช่น:

```
สวัสดีครับ/ค่ะ ผู้ปกครองทุกท่าน 🙏

ช่องทางนี้ใช้สำหรับรับข้อมูลจากห้องเรียนครับ

📅 ดูปฏิทินกิจกรรมได้ที่ช่อง #ปฏิทินกิจกรรม
   — บอทจะส่งรายการกิจกรรมวันนี้ทุกเช้า 06:00 น.

👗 ดูชุดแต่งกายวันนี้/พรุ่งนี้ได้ที่ช่อง #ชุดนักเรียน

🔔 ตั้งแจ้งเตือนส่วนตัวได้โดยพิมพ์ /remind ในช่องใดก็ได้
   (บอทจะส่ง DM แจ้งเตือนก่อนถึงวัน)

❓ มีคำถามพิมพ์ได้ที่ช่อง #ถามตอบ
```

---

#### 4. ปรับ Daily Reminder เป็น Embed (สวยงาม อ่านง่ายกว่า)
ปัจจุบัน daily reminder ส่งเป็น plain text เปลี่ยนเป็น `discord.Embed` จะดูเป็นมืออาชีพและอ่านง่ายกว่ามาก เช่น:

```python
embed = discord.Embed(
    title=f"🔔 กิจกรรมวันนี้ — {day} {month} {year}",
    color=0xF39C12,
)
for ev in today_events:
    embed.add_field(name=ev["name"], value=detail or cat_label, inline=False)
await channel.send("@everyone", embed=embed)
```

---

### 🟡 ระดับ 2 — ลงแรงปานกลาง ช่วยให้ประสบการณ์ดีขึ้นมาก

#### 5. ปุ่ม "แจ้งเตือนฉัน 🔔" บนข้อความปฏิทิน
แทนที่จะให้ผู้ปกครองพิมพ์ `/remind` ซึ่งต้องรู้ว่ามีคำสั่งนี้ ให้บอทแนบปุ่มไปกับข้อความ daily reminder เช่น:

```
[🔔 แจ้งเตือนฉัน 1 วันก่อน]  [🔔 แจ้งเตือนฉัน 1 ชั่วโมงก่อน]
```

ผู้ปกครองกดปุ่มได้เลย ไม่ต้องรู้คำสั่ง

**วิธีทำ:** ใช้ `discord.ui.View` กับ `discord.ui.Button` แนบไปกับ `channel.send()` ใน `daily_channel_reminder`

---

#### 6. `/my-reminders` command
ให้ผู้ปกครองดูว่าตัวเองตั้งแจ้งเตือนอะไรไว้บ้าง และยกเลิกได้ ปัจจุบันยังไม่มีคำสั่งนี้ ทำให้ผู้ปกครองที่ลืมว่าตั้งไว้แล้วจะกดซ้ำแล้วเห็นแค่ "คุณตั้งแจ้งเตือนไว้แล้ว" โดยไม่รู้ว่าตั้งไว้ตอนไหน

**โค้ดคร่าวๆ:**
```python
@tree.command(name="my-reminders", description="ดูการแจ้งเตือนที่ตั้งไว้")
async def my_reminders(interaction):
    reminders = [r for r in load_reminders()
                 if r["user_id"] == interaction.user.id and not r.get("sent")]
    # แสดงรายการ + ปุ่มยกเลิก
```

---

#### 7. Welcome DM เมื่อผู้ปกครองเข้า Server
เมื่อ member ใหม่เข้า server ให้บอท DM ส่งคำแนะนำการใช้งานสั้นๆ ไปหาเขาโดยอัตโนมัติ

**วิธีทำ:** เพิ่ม event handler `on_member_join` ใน `bot.py`:
```python
@client.event
async def on_member_join(member):
    try:
        await member.send("สวัสดีครับ/ค่ะ ยินดีต้อนรับสู่ห้องเรียน! ...")
    except discord.Forbidden:
        pass  # ผู้ใช้ปิด DM ก็ไม่เป็นไร
```

ต้องเพิ่ม `intents.members = True` ด้วย

---

### 🔴 ระดับ 3 — ลงแรงมาก แต่ impressive มาก

#### 8. ปุ่ม "ดูรายละเอียด" บนปฏิทิน
ปัจจุบันปฏิทินแสดงชื่อกิจกรรมแต่ไม่แสดง detail ใน image เพราะพื้นที่จำกัด เพิ่มปุ่มใต้ calendar image ให้กดดูรายละเอียดได้:

```
[📋 ดูรายละเอียดกิจกรรมทั้งหมด]
```

กดแล้วบอท reply เป็น ephemeral embed แสดง detail ของทุก event

---

#### 9. Thai-friendly date input สำหรับ `/add-event`
ตอนนี้ต้องพิมพ์ `2026-05-20` ซึ่ง Admin หลายคนสับสน เปลี่ยนให้รับ format แบบไทยด้วย เช่น `20/5/2569` หรือ `20 พ.ค.` โดย parse หลาย format แล้ว normalize เป็น `YYYY-MM-DD` ภายใน

---

#### 10. AI Q&A (`/ask`) — foundation พร้อมแล้ว
ตาม PROJECT_SUMMARY.md บอกว่า foundation พร้อมแล้ว (`resources.json` มีข้อมูลครบ) เพียงแต่ยังไม่ได้ implement `/ask` command นี้จะทำให้บอทดูน่าประทับใจมากสำหรับผู้ปกครอง เช่น:

```
ผู้ปกครอง: /ask กิจกรรมใกล้ๆ นี้มีอะไรบ้าง?
บอท: กิจกรรมที่กำลังจะมา: Field Trip ป.4 วันที่ 10 เมษายน...
```

---

## 📋 สรุป — ลำดับความสำคัญ

| ลำดับ | งาน | เวลาประมาณ | Impact |
|-------|-----|------------|--------|
| 1 | จัดโครงสร้าง channel + pin คำแนะนำ | 15 นาที | สูงมาก |
| 2 | เพิ่ม `/help` command | 30 นาที | สูง |
| 3 | เปลี่ยน daily reminder เป็น embed | 20 นาที | ปานกลาง |
| 4 | เพิ่มปุ่ม "แจ้งเตือนฉัน" บน daily reminder | 1-2 ชั่วโมง | สูงมาก |
| 5 | เพิ่ม `/my-reminders` | 1 ชั่วโมง | สูง |
| 6 | Welcome DM เมื่อเข้า server | 30 นาที | ปานกลาง |
| 7 | `/ask` AI Q&A | หลายชั่วโมง | สูงมาก |
